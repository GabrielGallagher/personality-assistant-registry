# Assistant Presets

Use this folder for full assistant presets (prompt + settings).
Include fields such as:
- `id`
- `name`
- `summary`
- `prompt`
- `defaults` (heartbeat, filesystem, etc.)
- optional `tags`
