# Local Filesystem MCP

Deprecated: use the core connector `filesystem` instead.

## Notes
- This legacy entry will be removed after one minor release window.
- The core filesystem connector is bundled with the desktop app and is always available.
